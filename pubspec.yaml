name: mood_journal
description: Um diário de humor e bem-estar para estudantes
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  flutter_svg: ^2.0.9
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  image_picker: ^1.0.4
  file_picker: ^6.1.0
  flutter_image_compress: ^2.1.0
  path_provider: ^2.1.1
  universal_html: ^2.2.3

  path: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/logo.svg
    - assets/logo_new.svg
